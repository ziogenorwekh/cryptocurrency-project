version: "3.9"

networks:
  my-network:
    driver: bridge

services:
  redis:
    image: redis:latest
    container_name: redis-server
    ports:
      - "10010:6379"
    networks:
      - my-network

  mysql:
    image: mysql:latest
    container_name: mysql-server
    ports:
      - "10020:3306"
    environment:
      MYSQL_ROOT_PASSWORD: choongang
    command: ["--default-authentication-plugin=mysql_native_password"]
    networks:
      - my-network